sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t tazlorbong/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true tazlorbong/docker-react npm run test
